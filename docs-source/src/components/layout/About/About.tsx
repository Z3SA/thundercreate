import styles from './About.module.scss';

export function About() {
  return (
    <article className={styles.About}>
      <p>
        Thundercreate  - adaptive flexbox grid with 20-column structure. This library includes settings of typography, normalize.css and grid itself.
      </p>
    </article>
  );
}